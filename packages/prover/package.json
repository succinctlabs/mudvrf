{
    "name": "prover",
    "version": "0.0.0",
    "private": true,
    "license": "MIT",
    "scripts": {
        "dev": "wait-port localhost:8545 && sleep 10 && go run main/main.go --vrfJsonPath $VRF_JSON_PATH"
    },
    "devDependencies": {
        "wait-port": "^1.0.4"
    }
}
  